# 大模型AI v1.3.0：用户使用说明

> 适用目录：`D:\Game\无名杀子琪懒人包v1.11.3-win32-x64\resources\app\extension\大模型AI`  
> 文档日期：2026-08-13  
> 当前状态：本轮程序目标和自动检查已经完成。

## 1. 这个扩展是做什么的

“大模型AI”让无名杀里的电脑角色在需要作选择时，把当前局面、规则、技能、聊天和最近发生的事交给大模型分析，再由扩展把模型的决定映射成游戏中的选牌、选目标或选按钮。

它不是简单地让模型看几句武将介绍。当前版本会尽量提供：

- 全场公开局面；
- 当前 AI 自己知道的手牌和技能；
- 当前战局中相关、有效且可知的技能中文说明和动态说明；
- 当前事件和合法候选；
- 最近的战局时间线；
- 玩家对 AI 说过的话及其作用范围；
- 原版 AI 的参考判断。

模型负责理解和计划，游戏本体负责最后的合法性检查。模型犯傻时，扩展不会硬猜或强行执行，而会撤销这次尚未结算的选择，并交给原版 AI 继续。

## 2. 当前版本已经完成的主要能力

### 2.1 更完整地理解技能和局势

行动模型会读取当前对局里与它有关、对它可知的技能内部 ID、中文名、中文规则说明和当前动态说明；也会看到当前玩家状态、装备、判定区、标记、自己手牌、事件链和最近战局历史。当前版本不再把技能的原始 JavaScript 源码发送给 API，以避免低难度选择也携带数万输入 Token。

输出 JSON 格式、稳定 ID、选牌/目标/按钮步骤都由扩展的系统契约和实时合法候选明确提供，不依赖模型阅读源码。因此停止发送源码不会让模型“不知道该输出什么”。最终能不能点、该选几个、目标顺序是否正确，仍由无名杀引擎实时判断。

### 2.2 聊天真正关联战局

旧版依靠固定关键词正则强制行动的系统已经删除。现在可以用正常中文和 AI 交流，例如：

- “这回合别打我。”
- “这次不要闪。”
- “以后优先保护 3 号。”
- “刚才为什么拆我的牌？”
- “大家这回合快点。”

聊天模型先判断这是不是行动意图、发给谁、作用于这次/本回合/本局以及涉及哪种选择；行动模型再结合原话、当前战况、时间线和合法候选决定。没有哪个固定词能百分之百像程序命令一样锁死 AI，这正是自然语言理解的取舍。

### 2.3 同一个动作尽量只思考一次

对于本体中同一个安全事件里的“先选牌或按钮，再选目标”，模型可以一次返回完整计划。例如刘备决定使用过河拆桥时，可在同一次思考里同时决定使用哪张牌和对谁使用，不必点牌后再请求模型重新思考目标。

这不是枚举“过河拆桥、杀、顺手牵羊”等常见牌。扩展按游戏事件结构处理，所以牌一视同仁。

当前 v1.3.0 已覆盖的安全连续流程是：

- 同事件 `选牌→选目标`；
- 同事件 `选按钮→选目标`；
- 动态有序多目标，例如先选 A 后第二目标才变得可选。
- 出牌阶段的本回合软连续计划：模型可在一次回答中列出当前动作之后准备继续使用的牌、技能和目标。

如果任一步和计划不一致，整次尚未结算的选择会撤销，再由原版 AI 从头完成。

本回合软连续计划和同事件事务不是一回事。同事件的选牌与目标尚未结算，可以整体撤销；出牌阶段的后一张牌则要等前一张真正结算后才进入新事件。扩展不会零间隔连点，而是按本体游戏速度调用自然延时，再用当时的真实候选映射下一步。普通掉血、失牌等小变化不会让计划立刻失忆；还能合法映射就继续，映射不了才把失败原因和剩余方针交给模型修正，不会盲点。

### 2.4 动态有序多目标

有些技能界面第一步只显示一个候选，选中后才出现第二步候选，但整个技能最终要求两个目标。现在模型会收到“目标数组顺序就是点击顺序”的明确说明，并一次返回完整目标序列；扩展逐项点击、每一步刷新候选。

模型若漏掉第二项或把顺序写反，属于可容忍的模型操作失败：扩展清理选择并交回原版 AI。扩展本身不能再把第一步快照误当成全部候选。

### 2.5 原版 AI 永远在旁边兜底

以下情况会使用原版 AI：

- API 未配置、网络失败或超时；
- 大模型返回的 JSON 无法解析；
- ID 不存在、数量不够、顺序不合法；
- 事件已经变化或控制权已经切换；
- 当前选择没有思考价值，例如强制弃牌或唯一候选；
- 设置命中了原版 AI 概率分流；
- 玩家手动把某个角色切回原版 AI；
- 扩展认知模块或本体桥接不安全。

因此，大模型偶尔作出笨决策不会把游戏卡死。如果回落后仍残留选中目标、原版 AI 无法继续，才更像扩展程序问题，应保存日志反馈。

### 2.6 每名 AI 都有本局连续记忆

普通 API 请求并不会天然记得上一次请求。扩展现在会按本局角色 ID，分别保存每名 AI 已经成功执行的动作和简短理由，并在它下一次行动时重新提供。

例如某 AI 使用杀时已经说明“若对方出闪，准备发动贯石斧”，之后进入贯石斧弃牌事件，它能看到自己刚才的行动方针，不必完全从零分析。普通的出闪、掉血、失牌或进入同一行动引发的技能事件，不会自动让记忆失效；如果关键对象已经不存在或当前候选根本无法延续，才重新判断。

这不是无限保存全部推理：只保留少量成功动作与结论，本回合内容优先，本局较早内容作背景；每名 AI 相互隔离。对局结束、开始新局或清空本局记忆时全部清除，绝不跨局。

模型还可以返回一个由稳定牌 ID、技能名、按钮 ID、目标 ID/座位组成的本回合 `steps` 计划。当前动作成功后，剩余步骤保存在对应角色的本局内存里；后续事件能直接匹配时无需再次请求模型，不能匹配时才把剩余方针交给模型局部修正。技能动态生成的按钮或代价牌仍以本体实时产生的候选为准，模型计划不是可以绕过规则的点击脚本。

对于无法提前写出稳定按钮 ID 的技能，仍可使用简短 `nextIntent`，例如“下一步优先选顺手牵羊并针对主公”。它是行动方针的补充，而不是固定分支脚本。

### 2.7 更稳定的提示缓存和思考提示

行动请求现在把长期不变的操作协议放在第一条 system 消息，思考设置、技能规则、局面、历史和候选依次放在后面。支持前缀缓存的 API 因而有更长、更稳定的公共开头；是否实际命中、命中多少仍由服务商决定，应以日志返回的缓存 Token 为准。

技能中文规则仍会完整提供，但归属、技能名和说明改用带一次格式说明的紧凑数组，避免为每个技能反复发送 `name/title/description` 等字段名。这是等信息压缩，不会删掉技能中文效果，也不会恢复发送 JavaScript 源码。

角色思考时会在头像上显示金色的“AI 思考中 N秒”。计时由程序持续更新，不依赖容易暂停的主题动画；连续两次思考也会区分新旧标签，旧请求不能再把新标签变白。标签应始终保持一行，末尾三个点不会单独换行。

思考标签只是附加文字。扩展不再屏蔽当前 AI 武将、候选目标、牌和按钮的本体/十周年 UI 高光，也不再关闭 AI 触发的伤害、回复、阵亡等视觉特效；点击武将时应继续显示主题原有反馈。

## 3. 安装和首次设置

1. 在无名杀中导入并启用“大模型AI”扩展。
2. 按提示等待游戏重启。扩展会检查本体版本，并安全安装所需桥接补丁和备份。
3. 打开“扩展 → 大模型AI”。
4. 填写自己的 API Key。
5. 填写服务商给出的 Base URL 和模型名。
6. 点击“测试 API 连接”。
7. 开一局游戏观察聊天框和日志页。

默认配置使用：

- Base URL：`https://api.deepseek.com`
- 模型：`deepseek-v4-flash`
- API Key：空，必须由用户自己填写

也可使用其他兼容 OpenAI Chat Completions 的中转或服务。扩展会调用 Base URL 下的 `POST /chat/completions`。

游戏设置里填写的 API Key 不会写进扩展目录的 `extension.js` 或 `config.json`，而是由无名杀写进 Electron/浏览器配置存储。这个懒人包在 `resources/app/main.js` 中把用户数据目录改到了包内的 `resources/app/Home/UserData`，其中 IndexedDB/Local Storage 可能含有已保存的 Key。

因此：只打包 `resources/app/extension/大模型AI` 扩展文件夹时，当前 `config.json` 的 Key 为空，不会顺带打包游戏内保存的 Key；若打包整个无名杀懒人包，则必须排除或先清理 `resources/app/Home/UserData`，否则可能泄露设置、登录状态及 API Key。日志设计上会脱敏 Key，但发布包仍建议不附 `log.txt` 和 `AI决策日志`。

### 3.1 导出和导入设置

扩展设置页提供“导出设置配置”和“导入设置配置”：

- 导出生成 UTF-8 JSON 文件，包含模型、地址、采样参数、思考设置、五类原版 AI 托管开关、日志与时间线等扩展设置；
- **绝不导出 API Key**，也不导出聊天记录、局内记忆、角色临时托管状态、运行日志或决策日志；
- 导入只接受当前扩展认识的设置键，未知键和类型错误项会被忽略；
- 即使导入文件里有人手工加入 `apiKey`，扩展也会忽略它，不覆盖本机已经保存的 Key；
- 导入前会显示将应用和忽略的项目数，确认后保存并重启游戏使全部设置一致生效；
- 也兼容直接以设置键为顶层的旧式 JSON，但建议保留正式导出文件的 `schema/schemaVersion/settings` 包装。

因此，分享这份导出的设置文件本身不会泄露扩展保存的 API Key；分享整个懒人包时仍须按上文检查 `Home/UserData`。

## 4. 重要设置怎么选

| 设置 | 默认 | 说明 |
|---|---:|---|
| 原版 AI 托管：出牌规划 | 关 | 当前武将主动使用牌/技能及其连续目标链是否直接交给原版 AI |
| 原版 AI 托管：技能与战术选择 | 关 | 未归入其他类别的技能、按钮、牌和目标选择是否交给原版 AI |
| 原版 AI 托管：响应与救援 | 开 | 闪、无懈、濒死救援、被要求使用牌等高频响应是否交给原版 AI |
| 原版 AI 托管：手牌与资源整理 | 开 | 弃牌、给牌、获得/弃置他人牌和移动整理牌是否交给原版 AI |
| 原版 AI 托管：强制或机械选择 | 开 | 强制弃牌、唯一合法项、必须全选等低思考价值选择是否交给原版 AI |
| Temperature | 0.25 | 0-2；越高越随机。它影响模型采样，不直接代表对局操作稳定性 |
| Top P | 1 | 0.01-1；越低，模型只从更集中的候选词范围采样 |
| 服务端推理档位 | low | 服务商支持时控制服务器推理强度 |
| 提示词思考深度 | 50% | 设置时填写 1-100；发送给模型、聊天框状态和日志均按百分数显示。这是提示模型权衡局面的软要求，不是服务端硬推理预算，也不保证实际思考 Token 随百分比线性变化 |
| 行动最大输出 Token | 8192 | 防止复杂模型来不及给出最终 JSON；不是每次都会用满 |
| 行动绝对时间上限 | 20 秒 | 从首次请求开始计算，所有重试共用这段时间 |
| 额外重试次数 | 2 | 首次 HTTP/API 失败后最多再试两次；逻辑非法通常直接回原 AI |
| 原版 AI 概率 | 0% | 普通选择随机直接走原版 AI，节省时间和 Token |
| AI 主动拟人发言概率 | 15% | 0-100；命中后让同一次行动回答附带一句武将口吻发言，不会额外请求 API |
| 原版 AI 提示参考程度 | 50% | 0-100%；按连续百分比告诉模型应给原版 AI 建议多大策略权重，每增加 1% 都按相同比例变化，不设低/中/高档或隐藏阈值。它不会在回答后否决或改写模型的合法选择 |
| 战局时间线最多条数 | 240 | 0 关闭；1-10000 保留最近记录，超出清理最旧部分 |
| 决策日志 | 开 | 保存模型行动理由和执行结果 |
| 归档日志保留数 | 20 | 超过后删除最旧归档 |
| 局内聊天允许 | 所有 AI | 可改为仅友方或仅敌方 |

Temperature、Top P、服务端推理档位、提示词思考深度和最大输出 Token 会发送给普通行动，也会用于玩家 `@` 某个武将后的聊天回复。聊天回复不再固定 `low`。“快点”模式只关闭本次服务端推理并缩短等待，不会强制把 Temperature 改成 0。API 连接测试不是对局决策，才会固定使用 Temperature=0。

例如“原版 AI 提示参考程度=35%”时，提示会明确写成“原版 AI 建议=35%，完整局面独立判断=65%”。这是一条给语言模型的连续权重指令，不是程序在后台计算总分；因此它能改变模型重视程度，但不能保证语言模型像数学公式一样精确混合。鲍信是否发动、纵马是否值得等策略问题不会写成本地特判，仍由模型结合这项参考与完整局面决定。

建议首次测试先保持默认，只确认 API、正常出牌和回落工作。若响应太慢，可减少最大输出 Token、提高原版 AI 概率，或让聊天说“这回合快点”。单独降低提示词思考深度不一定能让某些推理模型明显变快，应以日志里的实际推理 Token 和耗时为准。

## 5. 聊天框怎么用

### 5.1 发给谁

- 不写 `@`：默认发给当前行动 AI。
- 手动指定：从列表选择“武将名·N号”。
- 想让所有获准聊天的 AI 都知道：用“大家”“你们都”等自然表达，并由聊天模型判断对象。
- 聊天权限为仅友方/仅敌方时，被排除的 AI 不再读取旧聊天或执行旧的全体意图。

### 5.2 作用多久

可以自然表达：

- “这次”“这张”“这个”：当前事件；
- “这回合”：当前回合；
- “以后”“每次”“本局”：持续到本局结束。

作用域由聊天模型结构化判断。如果它误解了，属于模型理解问题；可以换一种更明确的说法，而不是期待固定口令。

### 5.3 “我”和“你”

- 玩家消息里的“我”指真人玩家。
- “你”指当前受令 AI。
- 建议目标角色写“3号”或从 @ 列表选择，避免同名角色歧义。

被聊天的 AI 知道自己的真实身份。扩展会提醒它：聊天会公开显示，直接说明身份、阵营或行动意图可能暴露自己；但是否坦白、伪装、试探或继续保密，由模型结合战局自行判断。扩展不再用本地关键词审查回复，也不会因为出现“忠臣、反贼、内奸”等字样而删除整句。其他角色尚未公开的身份仍不会作为确定事实提供给它。

行动和聊天提示还会明确当前主模式及子模式。例如身份模式会说明除主公等本体已公开角色外，其他身份通常从隐藏开始；斗地主会说明地主与农民属于公开阵营；国战会说明武将和势力可能暗置。模式规则只是背景，模型同时会看到本时点实际有多少身份已经公开，以及每名角色当前显示的身份。若一般模式说明和当前界面状态不同，以当前角色状态为准。

### 5.4 快速决策

“这次快点”“这回合快点”“本局都快点”会建立对应范围的速度意图。快速模式大约只给 1.5 秒并关闭推理，失败马上用原版 AI；它仍使用设置中的 Temperature 和 Top P。

“恢复正常速度”“不用急了”可按相同范围取消。

它和设置中的 20 秒行动超时不是同一件事。

### 5.5 按事件类别交给原版 AI

设置页提供五个互不依赖的托管开关。分类依据是无名杀当前选择事件的机制，不是枚举具体牌名或技能名：

- `出牌规划`：当前武将自己的主动出牌入口，以及同一主动动作的牌、按钮、目标链；
- `技能与战术选择`：没有归入其他类别的技能、按钮、牌、目标等战术选择；
- `响应与救援`：打出响应牌、无懈、濒死救援，以及别人要求该武将使用牌；
- `手牌与资源整理`：弃牌、给牌、获得/弃置他人牌、移动整理牌；
- `强制或机械选择`：强制弃牌、只剩唯一合法项、必须把剩余候选全部选完。

推荐组合也是新安装默认值：前两项关闭，后三项开启。这样主动出牌和主要战术仍由大模型思考，高频小选择直接由原版 AI，主要目的就是减少等待与 Token。五类都关闭等于所有可接管选择都尽量交给大模型；五类都开启等于所有这些选择都直接使用原版 AI。

若一个事件同时符合多类，优先按“强制机械 → 响应救援 → 资源整理 → 出牌规划 → 技能战术”归入唯一类别，避免两个开关互相打架。相关聊天指令不能越过已开启的托管边界；既然该类被设为原版 AI，就不会再为它请求模型。

旧版四档会自动迁移：`始终`变成五类全开，`关闭`变成五类全关，`出牌阶段内`只开启出牌规划，`出牌阶段外`则关闭出牌规划、开启其余四类。

点击聊天栏左侧机器人图标，可选择某个 AI 或全部 AI 由原版接管。控制权切换时，正在等待的旧大模型请求和未完成计划会作废，避免晚到结果操作错误角色。

切换造成的请求中止属于正常控制权变更，不是网络故障。运行日志应显示“正常取消”，不会重试，也不应出现 `The user aborted a request` 作为红色请求错误。

### 5.6 AI 主动拟人发言

设置中的“AI 主动拟人发言概率”可填 `0-100`。概率在每次真实模型行动开始时决定；命中后，模型在同一个 JSON 里附带一句 `speech`，不会为说话单独再花一次请求。只有相应行动最终确认成功才会发言，失败、过期或回落原版 AI 不会把计划中的话说出来。

发言会按当前武将的姓名、人物简介、技能和战况来扮演其口吻，同时受隐藏信息规则约束。它会显示为武将气泡并进入聊天页，等同于一条局内聊天记录；聊天框“日志”页不重复显示这句话，避免一条发言出现两次。完整 TXT 决策日志仍保留模型原始 `speech`，方便排错。

## 6. 战局时间线

时间线让行动和聊天不只看“此刻快照”，还知道最近发生了什么，例如谁对谁用了牌、谁受伤、谁回复、判定和公开牌移动。

为控制 Token 和内存，用户可以设置保留条数：

- `0`：关闭时间线；
- `1-10000`：保留最近相应数量；
- 超过上限：清理最旧记录；
- 手动清空本局聊天与记忆：旧记录不会从无名杀本体历史中重新出现；
- 对局结束：自动清空本局认知状态。

时间线不会把未知摸牌、对手暗手牌、暗将或私密技能泄露给不该知道的模型。聊天回复使用玩家发送消息时的战局锚点，所以 API 回复得晚，也不应把发送之后的新事情误当成“刚才”。

选择其他角色区域里的牌时也遵守同一规则：装备区、判定区以及已经展示或已知的手牌可显示牌面；普通暗手牌只显示为互不相同的“暗置牌”按钮。模型可以选择其中一张，但不会收到它的牌名、花色、点数、规则或由原版 AI 分数间接泄露的价值。

时间线是经过筛选的战略摘要，不是完整录像。

时间线记录“客观发生了什么”，本局行动记忆记录“这个 AI 当时想做什么”。行动决策使用两者保持连续性；公开聊天使用隐私过滤后的时间线和聊天记录关联刚才发生的事，不直接读取可能包含私有手牌判断的行动理由。

## 7. 技能规则和隐私

当前版本不向 API 发送技能的原始 JavaScript 源码。扩展仍以“在场角色实际持有的技能”和“当前事件动态/backup 技能”为根，追踪 `group/global/inherit/subSkill` 等关联，并只向模型提供这些当前相关技能的内部 ID、中文名、中文效果和动态说明。

这种做法同时保留两件重要能力：模型仍能知道技能做什么；扩展仍能通过合法候选、数量范围、事件协议和实时引擎检查告诉模型该怎样操作。原始源码曾是输入的主要大头之一，停止发送后能明显降低每次请求的逻辑输入量和首次缓存未命中的成本。

- 行动 AI 可以看到自己合法知道的技能和手牌。
- 其他角色只提供公开技能和公开状态。
- 公开聊天使用更保守的技能与事件视图。
- 每次实际点击仍要通过无名杀引擎的实时合法性检查。
- 每次决策会在当前局面附近附一份紧凑技能速查，直接列出相关技能的内部 ID、中文名、中文效果和当前动态说明，方便模型把角色与规则对应起来。

这样做的目标是让 AI 更懂技能，同时不变成全知透视 AI。

行动请求把固定操作协议放在最前方，技能与局面等动态信息放在后方，以提高前缀缓存成立的条件；扩展不依赖巨大的追加式源码目录换取缓存命中。是否真的命中仍要看日志返回的缓存 Token 和命中率，扩展不能代替服务商保证命中。

当前技能速查采用紧凑数组传输：`[技能ID, 中文名, 中文效果]`，动态说明采用 `[角色, 当前动态说明]`。字段名只解释一次，信息本身不减少。

运行日志中的 `[技能提示体积]` 会列出本次技能根数、关联定义数、`source_sent_chars=0` 和中文规则摘要字符数；`[提示组成]` 会列出每条消息的字符数。若输入再次异常增长，可据此确认不是技能源码被重新发送。

## 8. 两种日志分别有什么用

### 8.1 `log.txt`

这个文件**现在仍在使用，不是旧版废弃文件**。当前 v1.3.0 会继续向它追加：

- 扩展加载状态；
- 本体补丁检查；
- 回落原版 AI 的原因；
- 请求耗时和 Token 用量；
- 控制权、UI 和异常诊断。

旧记录可能因为历史编码显示乱码。游戏关闭时可以把它清空，扩展下次运行会自动重新创建；排查问题时不要先删，最好保存尾部内容。

### 8.2 `AI决策日志.txt`

它面向模型决策审计，通常在 `AI决策日志` 子目录中，内容包括：

- 当时局面和合法候选；
- 模型服务公开返回的 reasoning 字段；
- 最终 JSON 和中文理由；
- 扩展如何解析；
- 游戏实际执行还是交回原版 AI。

对局结束后会按时间归档，并按设置保留最近若干局。它不会记录 API Key，也不会伪造服务端未公开的完整思维。

遇到问题时，这个文件通常比只发截图更有用。

### 8.3 聊天框里的实时日志

聊天框“日志”页故意保持简洁，只显示：

- 思考设置（服务端档位、提示词深度、Temperature、Top P、最大输出）；
- 耗时、输入/输出/推理 Token；
- 缓存命中 Token 和命中率；
- 模型最终 JSON 和简短理由；
- 最终成功执行、主动跳过或交给原版 AI。

完整候选、上下文和长 reasoning 仍保存在 TXT，不会塞满聊天框。

## 9. `README.txt` 还有没有用

程序不会读取 `README.txt`，删掉不会影响运行。完成本说明和《AI接手指南》后，它的大部分内容确实重复。

当前建议仍然保留，因为：

- 老用户和导入扩展的人习惯先找 README；
- 它可以作为安装和快速入口；
- 以后可精简为几十行，只指向本说明和《AI接手指南》。

本轮没有删除或修改 README，避免只为整理文档扩大改动范围。

## 10. 以后改版后的回归检查清单

以后修改相关功能后，建议按这个顺序回归：

1. 打开扩展设置，测试 API 连接。
2. 导出设置，打开 JSON 确认没有 `apiKey`；临时改一个非敏感设置后重新导入，确认提示重启且本机 Key 未改变。
3. 开一局，确认 `log.txt` 显示核心桥接完整，聊天框正常出现。
4. 使用一张需要目标的普通牌，观察决策日志是否只有一次模型请求，并同时给出牌和目标。
5. 测试一个“先选 A、选后才出现 B”的动态双目标技能。
6. 在聊天里说“这回合别打我”，观察后续行动是否读取该语义。
7. 下一回合询问“你刚才为什么这样做”，观察是否能结合时间线回答。
8. 故意让模型返回错误 ID、漏目标或错误顺序，确认界面无残留并由原版 AI 继续。
9. 在两步选择之间切换原版 AI 事件托管，确认旧计划不会继续执行。
10. 对其他角色使用过河拆桥、顺手牵羊或“获得/弃置其牌”的技能，确认暗手牌只显示为“暗置牌”，而公开装备和判定牌仍显示名称。
11. 分别开关五类原版 AI 事件托管，确认每类只影响相应选择；重点覆盖主动出牌、闪/无懈/救援、弃牌/获得他人牌、强制唯一项和普通技能选择。
12. 完成一局太虚幻境，确认指示线和结算无报错。
13. 对局结束后检查决策日志归档和本局记忆清理。

如果测试失败，请保留：

- 最新 `AI决策日志.txt`；
- `log.txt` 最后几十行；
- 当时使用的牌/技能、角色座位和聊天原话；
- 相关设置；
- 是否切换过托管或原版 AI。

## 11. 常见问题

### 模型怎么又交给原版 AI 了？

先看决策日志中的“游戏实际执行”和 `log.txt`。常见原因是超时、格式错、ID 不存在、目标数量不够、顺序不合法、事件已变化或原版 AI 安全参考拒绝。

常见的三种正常回落应分开看：

1. 接口有推理但没有最终正文：模型输出失败；
2. 模型返回的稳定 ID、数量、目标顺序或实时游戏合法性不成立：模型操作失败；
3. `Failed to fetch`：网络失败。

只要扩展清干净本次选择、原版 AI 能正常接手，这三种都不需要为了“零回落”而修改程序。

单次模型犯傻可以容忍。如果同一合法流程持续因为“扩展根本没提供候选”而失败，才更像程序问题。

### 不发送技能源码后，模型会不会误解技能？

有可能，但发送完整源码也无法保证模型理解正确：许多技能还依赖闭包变量、动态 backup、临时 mod、随机结果和当时合法候选。当前选择是把更直接的中文规则与动态说明交给模型，把操作格式和合法性留给扩展与引擎；这比反复传送大量函数文本更省、更稳定。

个别技能在无名杀运行时会被 Proxy 包装。Proxy 可以理解为对象外面的一层拦截器：直接枚举可能触发作者自定义逻辑，所以保留的安全快照模块不会粗暴遍历。它通过无名杀自己的 `get.info(技能名)` 获取定义，只提取当前技能集合、中文名和中文效果用于请求，不把函数源码发给 API。模型若仍说“具体效果不明”，应查看 `activeSkillRules/dynamicSkillRules` 是否真的缺少说明，区分资料缺失和模型没有利用资料。

无名杀的技能说明还可能包含 `<noname-poptip>` 这种界面弹出提示标签。扩展清理 HTML 不是为了隐藏信息，而是去掉不能给模型直接使用的界面格式。当前会先把标签中已经明确引用的技能或卡牌恢复为 `〖中文名〗`/`【中文名】`，再清理其余标签；例如“获得〖趋袭〗至你下个准备阶段”不会再被误删成“获得至你下个准备阶段”。这只恢复原说明里已有的术语，不会枚举隐藏技能或开启全知视角。

### 为什么不是所有技能都一次点到底？

当前版本已经把同一安全事件里的选牌/按钮和目标合并，并能把出牌阶段的剩余动作保存为本回合软计划，按本体速度逐个续接。更长的技能流程若跨子事件，甚至在中间摸牌、判定、造成伤害或产生隐藏信息，就不能承诺像尚未结算的 UI 选择一样完整回滚；此时计划只能作为可校验、可局部修正的行动方针。

这不是技术上永远不能做，而是 v1.3.0 受时间、Token 和回归风险限制没有继续扩展。以后可以作为中等或更高复杂度功能继续实现。

当前的本局连续记忆和本回合 `steps` 能让跨事件的后续选择直接复用上次计划，明显减少重复推理；它仍不是可以预知随机结果的任意长条件脚本，每一步最终都要服从实时合法候选。

### 技能很多时还会不会很慢？

仍可能变慢，因为全场状态、中文技能规则、时间线、候选和行动记忆都需要发送；但最占体积的原始 JavaScript 技能源码已经取消。以后若成本再次异常，应先看 `[提示组成]` 和 `[技能提示体积]`，不要未经确认恢复源码发送。

### “AI 思考中”为什么以前会突然变白，三个点换行？

这是连续决策之间的 UI 生命周期竞态：上一次思考虽然已经返回，但延迟收尾稍后才运行；如果下一次思考已经开始，旧收尾会误删新标签依赖的样式类。v1.3.0 为每个角色记录当前标签所有者，旧请求只能清理自己的标签；标签本身也强制金色、横向排列和不换行。若仍复现，请提供截图和发生时相邻两条决策日志。

### 可以删 `log.txt` 吗？

可以在游戏关闭时清空旧内容，之后会自动重建；不建议在排错前删除。文件本身仍有用途。


## 12. 未来可以继续完善什么

在当前架构上，未来可以继续做：

- 对所有动态 backup、控制项和复杂技能子事件提供更完整的结构化计划图；
- 中途出现随机、隐藏信息或不可逆结算后的显式条件分支；
- 比当前软方针更精细的局部重新规划与计划解释；
- 更长时间线的自动摘要；
- 在当前稳定前缀和紧凑技能规则基础上，继续做更细的上下文体积统计与缓存管理；用户已明确不要“按决策复杂度自动限制推理预算”；
- 独立的前端回归测试工具。

这些是未来展望，不代表当前版本功能“只完成一半”。实施前应先评估复杂度和风险，再由用户决定是否值得做。

## 13. 扩展目录中的主要文件

| 文件 | 用途 |
|---|---|
| `extension.js` | 扩展主程序 |
| `game-timeline.cjs` | 战局时间线和隐私过滤 |
| `skill-source-snapshot.cjs` | 安全发现当前技能及实际规则组成关系；不会因 `sourceSkill` 来源标签或 `derivation` 衍生关系把非当前技能发给模型，且不发送原始源码 |
| `config.json` | 分发默认配置；API Key 应为空 |
| `info.json` | 扩展版本和说明 |
| `AI接手指南.md` | 给以后接手修改的 AI/开发者 |
| `用户使用说明.md` | 当前这份面向用户的说明 |
| `log.txt` | 当前仍使用的运行诊断日志 |
| `AI决策日志/` | 模型决策明细和按局归档 |

扩展还会安全补丁本体的 `content.js` 和 `game/index.js`，并在本体文件旁保存 `.bak-llm-ai` 备份和哈希元数据。不要手工乱删这些备份；关闭或删除扩展时，程序只会在确认安全的情况下自动还原。

---

对这个扩展最合适的期待是：**大模型负责更像人一样理解规则、局势、历史和聊天；无名杀引擎负责确保每一步合法；模型失败时原版 AI 可靠接手。**
